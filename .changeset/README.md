# Changesets

Hello and welcome! This folder has been automatically generated by `@changesets/cli`, a build tool that works
with multi-package repos, or single-package repos to help you version and publish your code. You can
find the full documentation for it [in the repository](https://github.com/changesets/changesets).

## Usage

1. **Add a changeset:** `pnpm changeset` — select packages and describe the change
2. **Version packages:** `pnpm changeset version` — bump versions and update changelogs
3. **Publish:** `pnpm changeset publish` — publish to npm
