## Parent

docs/prd-toast-refactor.md

## What to build

Update the `@beast-ui/system` semantic tokens (`semantic.css`) to introduce a `default` color role mapped to a neutral/muted tone from the Tactile Calm palette. This provides the CSS variables needed by all core components for their neutral states.

## Acceptance criteria

- [ ] `semantic.css` contains `--beast-color-default`, `--beast-color-default-light`, and `--beast-color-default-dark`.
- [ ] Values map to neutral/muted colors that fit the Botanical/Moss "Tactile Calm" palette.

## Blocked by

None - can start immediately
