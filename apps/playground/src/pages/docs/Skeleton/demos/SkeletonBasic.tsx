import { Skeleton } from '@beast-ui/core';

export default function SkeletonBasic() {
  return (
    <div>
      {/* TODO: Add basic usage for Skeleton */}
      <Skeleton>Basic Skeleton</Skeleton>
    </div>
  );
}
